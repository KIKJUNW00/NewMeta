package com.newmeta.service;

import com.newmeta.domain.Review;
import com.newmeta.persistence.ReviewRepository;

import lombok.RequiredArgsConstructor;

import java.util.Map;

import org.springframework.stereotype.Service;
import org.springframework.web.client.RestTemplate;

@Service
@RequiredArgsConstructor
public class ReviewService {

    private final ReviewRepository reviewRepository;

    public Review analyzeAndSaveReview(String review) {
        RestTemplate restTemplate = new RestTemplate(); // 외부 API 호출 도구
        String apiUrl = "http://10.125.121.116:8000/predict"; // FastAPI URLcv
        

//        // FastAPI에 데이터 전송 및 결과 받기
//        String prediction = restTemplate.postForObject(apiUrl, Map.of("review", review), String.class);

        // 데이터베이스에 저장
        Review reviewEntity = new Review();
        reviewEntity.setReview(review); // 변수명 변경
//        reviewEntity.setPrediction(prediction);
        return reviewRepository.save(reviewEntity);
    }
}